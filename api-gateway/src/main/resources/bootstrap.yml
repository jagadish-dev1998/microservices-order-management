#spring:
# application:
#  name: api-gateway
#  cloud:
#    config:
#      uri: http://localhost:8888
      
spring:
  application:
    name: api-gateway

  config:
    import: optional:configserver:http://localhost:8888

